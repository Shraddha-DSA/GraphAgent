[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "graphagent"
version = "0.1.0"
description = "Graph Neural Network powered Multi-Agent Workflow Optimizer"
requires-python = ">=3.11"